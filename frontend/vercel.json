{
  "buildCommand": "cd ../script && npm install && npm run build && cd ../frontend && npm run build",
  "devCommand": "npm run dev",
  "installCommand": "npm install",
  "framework": "nextjs",
  "outputDirectory": ".next"
}

